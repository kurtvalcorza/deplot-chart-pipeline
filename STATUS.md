# Release status

Current status: **Candidate — `E2E` tutorial source complete, supported-runtime execution outstanding**. The pipeline package with its adaptation contract (`predict`, `evaluate`, `adapt`, `save_artifact`, `load_artifact` / `from_artifact`), the carried `metrics.py` (relaxed position-wise cell accuracy, RNSS, exact-table match, the empty, header-only and medoid baselines) and `samples.py` (one digest-pinned SynthChartNet shard from `docling-project/SynthChartNet`, CDLA-Permissive-2.0, converted to DePlot's own table format and split 360 / 80 / 160 by chart type without sharing an image), the offline unit suites (pipeline, adaptation, role-helper, import-boundary and notebook-parity) and the model-backed adaptation suite on a small random Pix2Struct built offline from the committed config, VQA processor and tokenizer, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial `tutorials/deplot_chart_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks PAR1–PAR4). The shard's SHA-256 pin is recorded (`tools/pin_corpus.py`), and the pinned-checkpoint cases of the adaptation suite pass on CPU. The fine-tuning recipe has not been run end to end on the real checkpoint and corpus, so the notebook records `frozen_beats_empty` and `adapted_beats_frozen` instead of asserting them. The Kaggle and local runs in `docs/release-verification.md` were of the previous `TASK-INFERENCE` notebook and are not evidence for this revision; the repository is not release-grade until a Colab/Kaggle execution for the exact candidate blob is recorded there.
