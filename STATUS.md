# Release status

Current status: **Release-grade — exact-blob E2E run passed on Kaggle Tesla T4** (commit `efb92ad`, notebook blob `fb192936d3e6`, `kurtvalcorza/dimer-nb2-deplot-chart` v3, 2026-09-26 UTC, 4381.7 s, 11/11 post-restart code cells). The default seeded SynthChartNet split held 360 / 80 / 160 charts; on the 160-chart test split, relaxed position-wise cell accuracy was 0.160 frozen and 0.181 adapted, against 0.000 / 0.007 / 0.037 for the empty / header-only / medoid baselines. RNSS was 0.448 frozen and 0.447 adapted, and exact-table match was 0.006 and 0.000, so the +0.021 cell-accuracy result is not a uniform quality gain. The last two decoder blocks and final layer norm (18,879,744 of 282,285,696 parameters) trained for three epochs, with epoch 2 selected; the 29-tensor adapter reloaded with 8/8 identical tables. One seeded split of one shard on one runtime, no dispersion estimate, not a DePlot benchmark. The first T4 attempt on `60de8e1` failed on an embedded-module `_hub_download` namespace collision, fixed in `aada162` and regenerated in `efb92ad`. The full record and output digests are in `docs/release-verification.md`; any later notebook-blob change returns the carrier to **Candidate** until re-verified.
